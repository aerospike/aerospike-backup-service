run:
  timeout: 2m
  skip-dirs:
    - examples
    - modules
    - docs
    - docker

linters:
  disable-all: true
  enable:
    - dupl
    - errcheck
    - exportloopref
    - funlen
    - goconst
    - gocritic
    - gocyclo
    - gofmt
    - goimports
    - gosimple
    - govet
    - ineffassign
    - lll
    - misspell
    - prealloc
    - revive
    - staticcheck
    - stylecheck
    - typecheck
    - unconvert
    - unparam
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - unparam
        - prealloc
        - funlen
        - goconst
    - path: pkg/shared
      linters:
        - typecheck
    - path: pkg/stdio
      linters:
        - typecheck
    - path: pkg/shared
      text: "dupSubExpr"
      linters:
        - gocritic
    - path: pkg/shared
      linters:
        - lll
