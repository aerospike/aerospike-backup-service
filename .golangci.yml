run:
  timeout: 2m
linters:
  disable-all: true
  enable:
    - gci
    - bodyclose
    - unused
    - dogsled
    - dupl
    - errcheck
    - funlen
    - gci
    - goconst
    - gocritic
    - gocyclo
    - gofmt
    - goimports
    - gocyclo
    - gosec
    - gosimple
    - govet
    - ineffassign
    - misspell
    - prealloc
    - promlinter
    - nolintlint
    - nakedret
    - prealloc # pre-allocate slices with define size if the slice size is known in advance
    - predeclared
    - revive
    - sloglint
    - staticcheck
    - stylecheck
    - tagliatelle
    - thelper
    - tparallel
    - typecheck
    - unconvert
    - unparam
    - unused
    - whitespace
    - lll

linters-settings:
  gosec:
    excludes:
      - G115
  tagliatelle:
    case:
      rules:
        json: kebab
        yaml: kebab

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - unparam
        - prealloc
        - funlen
        - goconst

  exclude-dirs:
    - examples
    - modules
    - docs
    - docker
