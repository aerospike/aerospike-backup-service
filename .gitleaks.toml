# internal/tlsfixtures holds test-only TLS material generated by its generate.sh (self-signed
# certificates and keys protected by the password published in fixtures.go) and the Go
# package that lists those files by name. Nothing in it is a credential.
[extend]
useDefault = true

[allowlist]
paths = ['''internal/tlsfixtures/''']
