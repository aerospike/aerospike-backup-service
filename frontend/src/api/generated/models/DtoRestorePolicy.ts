/* tslint:disable */
/* eslint-disable */
/**
 * Backup Service REST API Specification
 * Aerospike Backup Service
 *
 * The version of the OpenAPI document: v3.4.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type {DtoCompressionPolicy} from "./DtoCompressionPolicy";
import {DtoCompressionPolicyFromJSON, DtoCompressionPolicyToJSON,} from "./DtoCompressionPolicy";
import type {DtoRestoreNamespace} from "./DtoRestoreNamespace";
import {DtoRestoreNamespaceFromJSON, DtoRestoreNamespaceToJSON,} from "./DtoRestoreNamespace";
import type {DtoRetryPolicy} from "./DtoRetryPolicy";
import {DtoRetryPolicyFromJSON, DtoRetryPolicyToJSON} from "./DtoRetryPolicy";
import type {DtoEncryptionPolicy} from "./DtoEncryptionPolicy";
import {DtoEncryptionPolicyFromJSON, DtoEncryptionPolicyToJSON,} from "./DtoEncryptionPolicy";

/**
 * RestorePolicy represents a policy for the restore operation.
 * @export
 * @interface DtoRestorePolicy
 */
export interface DtoRestorePolicy {
  /**
   * Throttles read operations from the backup file(s) to not exceed the given I/O bandwidth in MiB/s.
   * Default: no limit.
   * @type {number}
   * @memberof DtoRestorePolicy
   */
  bandwidth?: number | null;
  /**
   * The max allowed number of records per an async batch write call.
   * Only applicable when using batch writes.
   * @type {number}
   * @memberof DtoRestorePolicy
   */
  batchSize?: number;
  /**
   * The bins to restore (optional, an empty list implies restoring all bins).
   * @type {Array<string>}
   * @memberof DtoRestorePolicy
   */
  binList?: Array<string> | null;
  /**
   * Compression details (algorithm). Default is no compression.
   * @type {DtoCompressionPolicy}
   * @memberof DtoRestorePolicy
   */
  compression?: DtoCompressionPolicy;
  /**
   * Disables the use of batch writes when restoring records to the Aerospike cluster.
   * By default, the cluster is checked for batch write support.
   * @type {boolean}
   * @memberof DtoRestorePolicy
   */
  disableBatchWrites?: boolean;
  /**
   * Encryption details (algorithm and key). Default is no encryption.
   * @type {DtoEncryptionPolicy}
   * @memberof DtoRestorePolicy
   */
  encryption?: DtoEncryptionPolicy;
  /**
   * Amount of extra time-to-live to add to records that have expirable void-times.
   * Must be set in seconds.
   * @type {number}
   * @memberof DtoRestorePolicy
   */
  extraTtl?: number;
  /**
   * The max number of outstanding async record batch write calls at a time.
   * @type {number}
   * @memberof DtoRestorePolicy
   */
  maxAsyncBatches?: number;
  /**
   * Namespace optionally specifies an alternative namespace name for the restore operation.
   * By default, the data is restored to the namespace from which it was taken.
   * @type {DtoRestoreNamespace}
   * @memberof DtoRestorePolicy
   */
  namespace?: DtoRestoreNamespace;
  /**
   * Records from backups take precedence. This option disables the generation check.
   * With this option, records from the backup always overwrite records that already exist in
   * the namespace, regardless of generation numbers.
   * @type {boolean}
   * @memberof DtoRestorePolicy
   */
  noGeneration?: boolean;
  /**
   * Do not restore any secondary index definitions.
   * @type {boolean}
   * @memberof DtoRestorePolicy
   */
  noIndexes?: boolean;
  /**
   * Do not restore any record data (metadata or bin data).
   * By default, record data, secondary index definitions, and UDF modules will be restored.
   * @type {boolean}
   * @memberof DtoRestorePolicy
   */
  noRecords?: boolean;
  /**
   * Do not restore any UDF modules.
   * @type {boolean}
   * @memberof DtoRestorePolicy
   */
  noUdfs?: boolean;
  /**
   * The number of concurrent record readers from backup files.
   * This value controls the level of parallelism used by the backup service when
   * reading backup files.
   * The optimal value depends on hardware and network configuration.
   * @type {number}
   * @memberof DtoRestorePolicy
   */
  parallel?: number;
  /**
   * Replace records. This controls how records from the backup overwrite existing records in
   * the namespace. By default, restoring a record from a backup only replaces the bins
   * contained in the backup; all other bins of an existing record remain untouched.
   * @type {boolean}
   * @memberof DtoRestorePolicy
   */
  replace?: boolean;
  /**
   * Configuration of retries for each restore write operation.
   * If nil, the default policy is used (5 retries with a one-minute delay between attempts).
   * @type {DtoRetryPolicy}
   * @memberof DtoRestorePolicy
   */
  retryPolicy?: DtoRetryPolicy;
  /**
   * The sets to restore (optional, an empty list implies restoring all sets).
   * @type {Array<string>}
   * @memberof DtoRestorePolicy
   */
  setList?: Array<string> | null;
  /**
   * Timeout (ms) for Aerospike commands to write records, create indexes and create UDFs.
   * Socket timeout in milliseconds. Default is 10 minutes. If this value is 0, it is set to total-timeout.
   * If both are 0, there is no socket idle time limit.
   * @type {number}
   * @memberof DtoRestorePolicy
   */
  socketTimeout?: number;
  /**
   * Total socket timeout in milliseconds. Default is 0, that is, no timeout.
   * @type {number}
   * @memberof DtoRestorePolicy
   */
  totalTimeout?: number;
  /**
   * Throttles read operations from the backup file(s) to not exceed the given number of transactions per second.
   * Default: no limit.
   * @type {number}
   * @memberof DtoRestorePolicy
   */
  tps?: number | null;
  /**
   * Existing records take precedence. With this option, only records that do not exist in
   * the namespace are restored, regardless of generation numbers. If a record exists in
   * the namespace, the record from the backup is ignored.
   * @type {boolean}
   * @memberof DtoRestorePolicy
   */
  unique?: boolean;
}

/**
 * Check if a given object implements the DtoRestorePolicy interface.
 */
export function instanceOfDtoRestorePolicy(
  value: object,
): value is DtoRestorePolicy {
  return true;
}

export function DtoRestorePolicyFromJSON(json: any): DtoRestorePolicy {
  return DtoRestorePolicyFromJSONTyped(json, false);
}

export function DtoRestorePolicyFromJSONTyped(
  json: any,
  ignoreDiscriminator: boolean,
): DtoRestorePolicy {
  if (json == null) {
    return json;
  }
  return {
    bandwidth: json["bandwidth"] == null ? undefined : json["bandwidth"],
    batchSize: json["batch-size"] == null ? undefined : json["batch-size"],
    binList: json["bin-list"] == null ? undefined : json["bin-list"],
    compression:
      json["compression"] == null
        ? undefined
        : DtoCompressionPolicyFromJSON(json["compression"]),
    disableBatchWrites:
      json["disable-batch-writes"] == null
        ? undefined
        : json["disable-batch-writes"],
    encryption:
      json["encryption"] == null
        ? undefined
        : DtoEncryptionPolicyFromJSON(json["encryption"]),
    extraTtl: json["extra-ttl"] == null ? undefined : json["extra-ttl"],
    maxAsyncBatches:
      json["max-async-batches"] == null ? undefined : json["max-async-batches"],
    namespace:
      json["namespace"] == null
        ? undefined
        : DtoRestoreNamespaceFromJSON(json["namespace"]),
    noGeneration:
      json["no-generation"] == null ? undefined : json["no-generation"],
    noIndexes: json["no-indexes"] == null ? undefined : json["no-indexes"],
    noRecords: json["no-records"] == null ? undefined : json["no-records"],
    noUdfs: json["no-udfs"] == null ? undefined : json["no-udfs"],
    parallel: json["parallel"] == null ? undefined : json["parallel"],
    replace: json["replace"] == null ? undefined : json["replace"],
    retryPolicy:
      json["retry-policy"] == null
        ? undefined
        : DtoRetryPolicyFromJSON(json["retry-policy"]),
    setList: json["set-list"] == null ? undefined : json["set-list"],
    socketTimeout:
      json["socket-timeout"] == null ? undefined : json["socket-timeout"],
    totalTimeout:
      json["total-timeout"] == null ? undefined : json["total-timeout"],
    tps: json["tps"] == null ? undefined : json["tps"],
    unique: json["unique"] == null ? undefined : json["unique"],
  };
}

export function DtoRestorePolicyToJSON(json: any): DtoRestorePolicy {
  return DtoRestorePolicyToJSONTyped(json, false);
}

export function DtoRestorePolicyToJSONTyped(
  value?: DtoRestorePolicy | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value;
  }

  return {
    bandwidth: value["bandwidth"],
    "batch-size": value["batchSize"],
    "bin-list": value["binList"],
    compression: DtoCompressionPolicyToJSON(value["compression"]),
    "disable-batch-writes": value["disableBatchWrites"],
    encryption: DtoEncryptionPolicyToJSON(value["encryption"]),
    "extra-ttl": value["extraTtl"],
    "max-async-batches": value["maxAsyncBatches"],
    namespace: DtoRestoreNamespaceToJSON(value["namespace"]),
    "no-generation": value["noGeneration"],
    "no-indexes": value["noIndexes"],
    "no-records": value["noRecords"],
    "no-udfs": value["noUdfs"],
    parallel: value["parallel"],
    replace: value["replace"],
    "retry-policy": DtoRetryPolicyToJSON(value["retryPolicy"]),
    "set-list": value["setList"],
    "socket-timeout": value["socketTimeout"],
    "total-timeout": value["totalTimeout"],
    tps: value["tps"],
    unique: value["unique"],
  };
}
